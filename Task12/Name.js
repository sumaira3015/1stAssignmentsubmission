"use strict";
let names = [`Sumaira`, `aqsa`, `jaweria`, `mahanoor`, `aliza`, `mehwish`];
const message = `Welcome to typescript programming : `;
for (let i = 0; i < names.length; i++) {
    console.log(message + names[i]);
}
